/**
 * 插件配置定义
 */

export interface SlaveMarketConfig {
    // 基础配置
    初始余额: number;
    初始身价: number;
    初始存款上限: number;
    管理员列表: string[];

    // 经济配置
    打工收益比例: number;
    抢劫收益比例: number;
    存款利率: number;
    转账手续费: number;

    // 贷款配置
    贷款系统: {
        基础额度: number;
        等级加成: number;
        利率: number;
    };

    // 冷却时间（毫秒）
    冷却时间: {
        打工: number;
        抢劫: number;
        转账: number;
        购买: number;
        种地: number;
        收获: number;
    };

    // 种地配置
    种地系统: {
        地块价格: number[];  // 每块地的价格
        最大地块数: number;
    };

    // VIP配置
    VIP配置: {
        启用: boolean;
        管理员永久VIP: boolean;
    };

    // 调试
    调试日志: boolean;
}

export const defaultConfig: SlaveMarketConfig = {
    初始余额: 0,
    初始身价: 100,
    初始存款上限: 1000,
    管理员列表: [],

    打工收益比例: 0.1,
    抢劫收益比例: 0.3,
    存款利率: 0.01,
    转账手续费: 0.05,

    贷款系统: {
        基础额度: 1000,
        等级加成: 500,
        利率: 0.02,
    },

    冷却时间: {
        打工: 120000,      // 2分钟
        抢劫: 60000,       // 1分钟
        转账: 120000,      // 2分钟
        购买: 300000,      // 5分钟
        种地: 30000,       // 30秒
        收获: 10000,       // 10秒
    },

    种地系统: {
        地块价格: [10000, 50000, 100000, 200000, 400000, 800000, 1600000, 3200000, 6400000, 12800000],
        最大地块数: 10,
    },

    VIP配置: {
        启用: true,
        管理员永久VIP: true,
    },

    调试日志: false,
};
